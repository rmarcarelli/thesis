from setuptools import setup, find_packages

setup(
    name="thesis_code",
    version="0.1.0",
    author="Roman Marcarelli",
    description="Computational framework for some aspects of charged lepton flavor violation in high-energy physics.",
    packages=find_packages(),
    install_requires=[],  # Add dependencies here if needed
    python_requires=">=3.7",
) 